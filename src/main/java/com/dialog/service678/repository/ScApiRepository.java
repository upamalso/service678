package com.dialog.service678.repository;

import com.dialog.service678.entity.ScApi;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScApiRepository extends JpaRepository<ScApi, Long> {
}
